// ARRAY
// ◽ used to store value/data of different data types - it is heterogeous ["hi", 477, 8.099, false] ✅
// ◽ here no need to predefine the size of array

// ----- IN JAVA -----
// int[] arr = new Int[5];
// arr = {1, 3, 5, 7, 9};
// in JAVA - "new" keyword for object creation

// ◽ Declaration of Array

let arr = [10, "hello", true, 1n, undefined];
console.log(arr);

// ◽ new keyword

let arr1 = new Array(10, 20, 30);   // new ---> then ---> Array constructor which is --> Array
                                    // new keywords creates the object using constructor
console.log(arr1);
